---
import Layout from "../layouts/Layout.astro";
const posts = Object.values(import.meta.glob("../content/blog/*.md", { eager: true }));
---
<Layout
   title="Clyron">
   <div class="text-3xl font-semibold m-1" id="greeting">Blog Posts</div>
   <div class="text-base">
      <p>My space for more long-form thoughts.</p>
   </div>
    Not implemented. Check back later.
</Layout>